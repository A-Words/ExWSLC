# 99：统一只读复查 ExWSLC 的 WSLC 升级任务

请对 ExWSLC 的 WSLC 升级实现做一次只读审查。主仓库位于 E:/src/personal/ExWSLC；请按我提供的工作树／分支／提交定位实际代码，不假设主工作树就是最终实现。

我会提供以下信息，可在发送时填写：
- 待检查任务编号：
- 公共基线 SHA：
- 各工作树绝对路径、分支、起始基线与最终提交 SHA：
- 如已有整合结果，集成工作树／分支／HEAD：
- 各任务验证文档位置：

如果信息未完全填写，先用 git worktree list、各工作树的 status／log 和 docs/validation/wslc-*.md 定位候选。只有无法可靠判定待审范围时，再指出缺少的具体信息；不要混入无关分支。

## 审查范围

1. 阅读 AGENTS.md、相关提示词、实现与验收记录；验证前置任务是否进入各分支基线。检查基线以来的完整 diff，以及 staged、unstaged、untracked 内容，不只看最后一次提交或任务总结。
2. 检查 01 的旧数组／新逐行对象／单对象／空输出解析，资源错误与空清单区分、状态／ID／端口字段及库存刷新保持。
3. 检查 02 的能力约定、CLI／服务／SDK 版本区分、命令探测回退、安装 API 适配。确认各分支复用同一套能力模型，不把未发行或未知能力当作已支持。
4. 对已完成的功能逐项检查：健康检查配置与状态；网络连接／断开与缓存；文件复制路径和覆盖语义；构建 secret／输出模式；停止超时与挂载；诊断脱敏；宿主机回环检查；刷新调度和空闲策略。
5. 重点查跨任务交互：容器创建是否保留全部选项、接口与设计时实现是否同步、容器切换与异步结果绑定、任务取消、日志跟随、详情缓存、最小化恢复、并发操作以及资源更新。
6. 检查共享文件的合并结果，包括 IContainerRuntime、WslcContainerRuntime、DesignContainerRuntime、RuntimeCapabilities、双语资源与公共 Mock。关注接口重命名、重载冲突、丢失分支改动、重复资源键和原有功能回归。
7. 如果 11／12 声称已实现，重新核对确切发行版与公开入口，确认原生重启回退不会重复执行或删除 --rm 容器，事件订阅有取消、重连、合并与轮询回退。
8. 没有集成工作树时，可以审查各分支及潜在接口冲突，但必须明确“跨分支整体行为尚未验证”；不能把逐分支测试通过当成整合后通过。

## 验证方式

- 保持源码、分支和历史只读：不修复、不自动合并／rebase、不提交、不 push、不 stash 用户改动。构建／测试生成的正常输出文件不等于允许编辑实现。
- 在确切的待审基线／集成工作树上运行适合的 build、test 和 diff 检查，记录命令和结果。已有失败要区分本次引入与环境／既存问题。
- WSLC 查询可以只读核对；不为审查自动修改容器、网络、卷、原生配置或升级运行时。对需要真实变更的验收查看各任务记录，缺少的实机证据明确指出。
- 对 UI 检查中英文资源、绑定、布局及截图证据。有条件时实际启动检查，不能把截图或源码检查冒充完整交互验收。
- 查看真实代码和命令结果，不把另一个会话的“已完成”或“测试通过”直接当作证据。

## 输出格式

先给出可复现、可行动的问题，按严重性排序。每条包含优先级、任务编号、准确文件与行号、触发条件、影响和证据；不要用纯风格建议凑问题。

随后用简短表格列出各任务完成度、实际验证覆盖和剩余边界。单独列出未集成／未实机验证项，并指出下一步需要的具体分支或运行条件。

如果没有确认的问题，直接说明没有发现可确认的问题，同时写清审查范围和测试边界；不要宣称未测试的平台或组合已经就绪。修复留待我明确要求后再执行。
