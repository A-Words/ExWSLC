<Page x:Class="ExWSLC.Views.Pages.TasksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:designViewModels="clr-namespace:ExWSLC.ViewModels.Design"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=designViewModels:DesignTasksViewModel, IsDesignTimeCreatable=True}"
      Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
    <Grid Margin="24,18,24,24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
        <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{DynamicResource TasksTitle}" Style="{StaticResource PageTitleStyle}"/><TextBlock Text="{DynamicResource TasksSubtitle}" Style="{StaticResource PageSubtitleStyle}"/></StackPanel><WrapPanel Grid.Column="1"><ui:Button Content="{DynamicResource CancelCurrent}" Icon="{ui:SymbolIcon Symbol=Stop24}" Command="{Binding CancelCurrentOperationCommand}" Appearance="Secondary" Margin="0,0,8,0" VerticalAlignment="Top"/><ui:Button Content="{DynamicResource ClearCompleted}" Icon="{ui:SymbolIcon Symbol=Delete24}" Command="{Binding ClearTasksCommand}" Appearance="Secondary" VerticalAlignment="Top"/></WrapPanel></Grid>
        <Border Grid.Row="1" Style="{StaticResource LayerCardStyle}" Margin="0" Padding="8">
            <ui:ListView ItemsSource="{Binding Tasks}">
                <ui:ListView.ItemTemplate><DataTemplate><Border Style="{StaticResource CompactCardStyle}" Margin="2,2,2,8"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="120"/><ColumnDefinition Width="3*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions><ui:ProgressRing Width="24" Height="24" IsIndeterminate="True" Margin="0,0,14,0"><ui:ProgressRing.Style><Style TargetType="ui:ProgressRing"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding State}" Value="Running"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></ui:ProgressRing.Style></ui:ProgressRing><StackPanel Grid.Column="1"><TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="{Binding StartedAt}" Style="{StaticResource MutedTextStyle}" Margin="0,3,0,0"/></StackPanel><Border Grid.Column="2" Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" CornerRadius="5" Padding="9,4" HorizontalAlignment="Left" VerticalAlignment="Center"><TextBlock Text="{Binding State}"/></Border><TextBlock Grid.Column="3" Text="{Binding Detail}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Opacity=".7"/><TextBlock Grid.Column="4" Text="{Binding FinishedAt}" VerticalAlignment="Center" Opacity=".6"/></Grid></Border></DataTemplate></ui:ListView.ItemTemplate>
            </ui:ListView>
        </Border>
    </Grid>
</Page>
