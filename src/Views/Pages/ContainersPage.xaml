<Page x:Class="ExWSLC.Views.Pages.ContainersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      x:Name="RootPage"
      Background="Transparent"
      Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
    <Page.Resources>
        <Style x:Key="WorkbenchPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{ui:ThemeResource ControlFillColorSecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{ui:ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
        </Style>

        <Style x:Key="MetricTileStyle" TargetType="Border" BasedOn="{StaticResource WorkbenchPanelStyle}">
            <Setter Property="Padding" Value="14" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>

        <Style x:Key="ContainerCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ui:ThemeResource ControlFillColorSecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{ui:ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="18,14" />
            <Setter Property="Margin" Value="2,4,2,8" />
        </Style>

        <Style x:Key="ContainerMetricPillStyle" TargetType="Border">
            <Setter Property="Background" Value="{ui:ThemeResource ControlFillColorTertiaryBrush}" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Margin" Value="0,8,8,0" />
        </Style>

        <Style x:Key="ContainerActionButtonStyle" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <DataTemplate x:Key="ContainerListItemTemplate">
            <Border Style="{StaticResource ContainerCardStyle}">
                <Grid MinHeight="72">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <Border Width="8" Height="8" CornerRadius="4" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,8,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{ui:ThemeResource TextFillColorTertiaryBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Background" Value="{ui:ThemeResource SystemFillColorSuccessBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>

                        <StackPanel Grid.Column="1" MinWidth="0" Margin="0,0,14,0">
                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Image}" Style="{StaticResource MutedTextStyle}" FontSize="13" TextTrimming="CharacterEllipsis" Margin="0,4,0,0" />

                            <WrapPanel>
                                <Border Style="{StaticResource ContainerMetricPillStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{DynamicResource Ports}" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding Ports}" FontSize="12" TextTrimming="CharacterEllipsis" MaxWidth="180" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource ContainerMetricPillStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{DynamicResource Cpu}" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding Cpu}" FontSize="12" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource ContainerMetricPillStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{DynamicResource Memory}" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding Memory}" FontSize="12" />
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="18,0,0,0">
                        <ui:Button Icon="{ui:SymbolIcon Symbol=Play24}" Appearance="Secondary" Command="{Binding DataContext.StartContainerFromListCommand, ElementName=RootPage}" CommandParameter="{Binding Container}" ToolTip="{DynamicResource Start}">
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource ContainerActionButtonStyle}">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                        <ui:Button Icon="{ui:SymbolIcon Symbol=Stop24}" Appearance="Secondary" Command="{Binding DataContext.StopContainerFromListCommand, ElementName=RootPage}" CommandParameter="{Binding Container}" ToolTip="{DynamicResource Stop}">
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource ContainerActionButtonStyle}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                        <ui:Button Icon="{ui:SymbolIcon Symbol=MoreHorizontal24}" Appearance="Secondary" Command="{Binding DataContext.ShowContainerDetailsFromListCommand, ElementName=RootPage}" CommandParameter="{Binding Container}" ToolTip="{DynamicResource Actions}" Style="{StaticResource ContainerActionButtonStyle}" />
                        <ui:Button Icon="{ui:SymbolIcon Symbol=Delete24}" Appearance="Danger" Command="{Binding DataContext.RemoveContainerFromListCommand, ElementName=RootPage}" CommandParameter="{Binding Container}" ToolTip="{DynamicResource Remove}" Style="{StaticResource ContainerActionButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="ContainerListTemplate">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition /></Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <ui:TextBox PlaceholderText="{DynamicResource Search}" ClearButtonEnabled="True" Text="{Binding DataContext.SearchText, ElementName=RootPage, UpdateSourceTrigger=PropertyChanged}" MinHeight="28" VerticalContentAlignment="Center" />
                    <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon Symbol=ArrowSync24}" Appearance="Secondary" Command="{Binding DataContext.RefreshAllCommand, ElementName=RootPage}" Width="28" Height="28" Padding="0" Margin="8,0,0,0" ToolTip="{DynamicResource Refresh}" />
                    <ui:Button Grid.Column="2" Icon="{ui:SymbolIcon Symbol=Add24}" Appearance="Secondary" Command="{Binding DataContext.ShowCreateContainerCommand, ElementName=RootPage}" Width="28" Height="28" Padding="0" Margin="8,0,0,0" ToolTip="{DynamicResource CreateContainer}" />
                </Grid>
                <ScrollViewer Grid.Row="1"
                              Margin="0,14,0,0"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding DataContext.VisibleContainerItems, ElementName=RootPage}"
                                  ItemTemplate="{StaticResource ContainerListItemTemplate}" />
                </ScrollViewer>
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="360">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.VisibleContainerItems.Count, ElementName=RootPage}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.SearchText, ElementName=RootPage}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="{DynamicResource NoContainersTitle}" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,7" />
                        <TextBlock Text="{DynamicResource NoContainersHint}" TextWrapping="Wrap" TextAlignment="Center" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                        <ui:Button Content="{DynamicResource CreateContainer}" Icon="{ui:SymbolIcon Symbol=Add24}" Appearance="Primary" Command="{Binding DataContext.ShowCreateContainerCommand, ElementName=RootPage}" HorizontalAlignment="Center" Margin="0,18,0,0" />
                    </StackPanel>

                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.SearchText, ElementName=RootPage}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="{DynamicResource NoContainerResultsTitle}" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,7" />
                        <TextBlock Text="{DynamicResource NoContainerResultsHint}" TextWrapping="Wrap" TextAlignment="Center" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="ContainerCreateTemplate">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="26">
                    <DockPanel>
                        <ui:Button DockPanel.Dock="Right" Icon="{ui:SymbolIcon Symbol=Dismiss24}" Appearance="Secondary" Command="{Binding DataContext.CancelCreateContainerCommand, ElementName=RootPage}" VerticalAlignment="Top" />
                        <StackPanel>
                            <TextBlock Text="{DynamicResource CreateContainer}" FontSize="28" FontWeight="SemiBold" />
                            <TextBlock Text="{DynamicResource CreateContainerHint}" Style="{StaticResource PageSubtitleStyle}" Margin="0,4,0,18" />
                        </StackPanel>
                    </DockPanel>
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock Text="{DynamicResource Image}" />
                                <ui:TextBox Text="{Binding DataContext.NewImage, ElementName=RootPage, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="ubuntu:latest" Margin="0,5,0,12" />
                                <TextBlock Text="{DynamicResource Name}" />
                                <ui:TextBox Text="{Binding DataContext.NewContainerName, ElementName=RootPage, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="my-container" Margin="0,5,0,12" />
                                <TextBlock Text="{DynamicResource Command}" />
                                <ui:TextBox Text="{Binding DataContext.NewCommand, ElementName=RootPage, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="{DynamicResource OptionalCommand}" Margin="0,5,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{DynamicResource Cpu}" />
                                <ui:TextBox Text="{Binding DataContext.NewCpuLimit, ElementName=RootPage}" PlaceholderText="1.5" Margin="0,5,0,12" />
                                <TextBlock Text="{DynamicResource Memory}" />
                                <ui:TextBox Text="{Binding DataContext.NewMemoryLimit, ElementName=RootPage}" PlaceholderText="512M" Margin="0,5,0,12" />
                                <TextBlock Text="{DynamicResource Network}" />
                                <ui:TextBox Text="{Binding DataContext.NewNetwork, ElementName=RootPage}" PlaceholderText="bridge" Margin="0,5,0,0" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Advanced}" Style="{StaticResource SectionTitleStyle}" />
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                                <StackPanel Margin="0,0,12,0"><TextBlock Text="{DynamicResource EnvironmentVariables}" /><ui:TextBox Text="{Binding DataContext.NewEnvironment, ElementName=RootPage}" AcceptsReturn="True" Height="76" PlaceholderText="KEY=VALUE" /></StackPanel>
                                <StackPanel Grid.Column="1" Margin="0,0,12,0"><TextBlock Text="{DynamicResource Ports}" /><ui:TextBox Text="{Binding DataContext.NewPorts, ElementName=RootPage}" AcceptsReturn="True" Height="76" PlaceholderText="8080:80" /></StackPanel>
                                <StackPanel Grid.Column="2"><TextBlock Text="{DynamicResource Volumes}" /><ui:TextBox Text="{Binding DataContext.NewVolumes, ElementName=RootPage}" AcceptsReturn="True" Height="76" PlaceholderText="source:/target" /></StackPanel>
                            </Grid>
                            <Grid Margin="0,14,0,0">
                                <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                                <StackPanel Margin="0,0,12,0"><TextBlock Text="{DynamicResource User}" /><ui:TextBox Text="{Binding DataContext.NewUser, ElementName=RootPage}" PlaceholderText="1000:1000" /></StackPanel>
                                <StackPanel Grid.Column="1" Margin="0,0,12,0"><TextBlock Text="{DynamicResource Workdir}" /><ui:TextBox Text="{Binding DataContext.NewWorkingDirectory, ElementName=RootPage}" PlaceholderText="/app" /></StackPanel>
                                <StackPanel Grid.Column="2" VerticalAlignment="Bottom"><CheckBox Content="{DynamicResource AllGpus}" IsChecked="{Binding DataContext.NewUseAllGpus, ElementName=RootPage}" /><CheckBox Content="{DynamicResource RemoveWhenStopped}" IsChecked="{Binding DataContext.NewRemoveWhenStopped, ElementName=RootPage}" Margin="0,8,0,0" /></StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    <ui:Button Content="{DynamicResource Run}" Icon="{ui:SymbolIcon Symbol=Play24}" Appearance="Primary" Command="{Binding DataContext.RunNewContainerCommand, ElementName=RootPage}" Padding="24,9" HorizontalAlignment="Right" />
                </StackPanel>
            </ScrollViewer>
        </DataTemplate>

        <DataTemplate x:Key="ContainerDetailTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Margin="0,0,0,12">
                    <ui:Button Content="{DynamicResource ContainersTitle}" Icon="{ui:SymbolIcon Symbol=ArrowLeft24}" Appearance="Secondary" Command="{Binding DataContext.ShowContainerListCommand, ElementName=RootPage}" />
                </DockPanel>

                <Border Grid.Row="1" Style="{StaticResource WorkbenchPanelStyle}" Padding="22" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Border Width="78" Height="78" CornerRadius="16" Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" Margin="0,0,20,0">
                            <ui:FontIcon FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xF5ED;" FontSize="40" Foreground="{ui:ThemeResource SystemFillColorSuccessBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontSize="28" FontWeight="SemiBold" />
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Border Width="8" Height="8" CornerRadius="4" Background="{ui:ThemeResource SystemFillColorSuccessBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                <TextBlock Text="{Binding State}" FontSize="14" />
                                <TextBlock Text="  |  " Opacity=".45" />
                                <TextBlock Text="{Binding Status}" FontSize="14" />
                                <TextBlock Text="  |  ID: " Opacity=".45" />
                                <TextBlock Text="{Binding Id}" FontFamily="Consolas" FontSize="13" TextTrimming="CharacterEllipsis" MaxWidth="180" />
                            </StackPanel>
                        </StackPanel>
                        <WrapPanel Grid.Column="2" VerticalAlignment="Top">
                            <ui:Button Content="{DynamicResource Terminal}" Icon="{ui:SymbolIcon Symbol=WindowConsole20}" Appearance="Secondary" Command="{Binding DataContext.OpenTerminalCommand, ElementName=RootPage}" Margin="0,0,8,0" />
                            <ui:Button Content="{DynamicResource Logs}" Icon="{ui:SymbolIcon Symbol=DocumentText24}" Appearance="Secondary" Command="{Binding DataContext.ShowLogsCommand, ElementName=RootPage}" Margin="0,0,8,0" />
                            <ui:Button Content="{DynamicResource Exec}" Icon="{ui:SymbolIcon Symbol=ChevronRight24}" Appearance="Secondary" Command="{Binding DataContext.ExecCommand, ElementName=RootPage}" />
                        </WrapPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource WorkbenchPanelStyle}" Padding="16,12" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{DynamicResource Image}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="{Binding Image}" Margin="12,0,28,0" TextTrimming="CharacterEllipsis" MaxWidth="340" />
                            <TextBlock Text="{DynamicResource Ports}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="{Binding Ports, TargetNullValue=-}" Margin="12,0,0,0" TextTrimming="CharacterEllipsis" MaxWidth="220" />
                        </StackPanel>
                        <WrapPanel Grid.Column="1">
                            <ui:Button Content="{DynamicResource Start}" Icon="{ui:SymbolIcon Symbol=Play24}" Appearance="Primary" Command="{Binding DataContext.StartContainerCommand, ElementName=RootPage}" Margin="0,0,8,0" />
                            <ui:Button Content="{DynamicResource Stop}" Command="{Binding DataContext.StopContainerCommand, ElementName=RootPage}" Margin="0,0,8,0" />
                            <ui:Button Content="{DynamicResource Restart}" Icon="{ui:SymbolIcon Symbol=ArrowClockwise24}" Command="{Binding DataContext.RestartContainerCommand, ElementName=RootPage}" Margin="0,0,8,0" />
                            <ui:Button Content="{DynamicResource Remove}" Appearance="Danger" Command="{Binding DataContext.RemoveContainerCommand, ElementName=RootPage}" />
                        </WrapPanel>
                    </Grid>
                </Border>

                <TabControl Grid.Row="3" Margin="0">
                    <TabItem Header="{DynamicResource OverviewTab}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <Grid Margin="0,14,0,0">
                                <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="16" /><ColumnDefinition /></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <Border Style="{StaticResource MetricTileStyle}">
                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{DynamicResource Cpu}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" /><TextBlock Text="{Binding DataContext.SelectedContainerStats.Cpu, ElementName=RootPage, TargetNullValue=--}" FontSize="22" Margin="0,4,0,0" /></StackPanel><ui:SymbolIcon Grid.Column="1" Symbol="Pulse24" FontSize="24" Foreground="{DynamicResource AccentFillColorDefaultBrush}" /></Grid>
                                    </Border>
                                    <Border Style="{StaticResource MetricTileStyle}">
                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{DynamicResource Memory}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" /><TextBlock Text="{Binding DataContext.SelectedContainerStats.Memory, ElementName=RootPage, TargetNullValue=--}" FontSize="22" Margin="0,4,0,0" /></StackPanel><ui:SymbolIcon Grid.Column="1" Symbol="Database24" FontSize="24" Foreground="{DynamicResource AccentFillColorDefaultBrush}" /></Grid>
                                    </Border>
                                    <Border Style="{StaticResource MetricTileStyle}">
                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{DynamicResource NetworkIo}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" /><TextBlock Text="{Binding DataContext.SelectedContainerStats.NetworkIo, ElementName=RootPage, TargetNullValue=--}" FontSize="22" Margin="0,4,0,0" /></StackPanel><ui:SymbolIcon Grid.Column="1" Symbol="ArrowBidirectionalUpDown24" FontSize="24" Foreground="{DynamicResource AccentFillColorDefaultBrush}" /></Grid>
                                    </Border>
                                    <Border Style="{StaticResource MetricTileStyle}" Margin="0">
                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{DynamicResource BlockIo}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" /><TextBlock Text="{Binding DataContext.SelectedContainerStats.BlockIo, ElementName=RootPage, TargetNullValue=--}" FontSize="22" Margin="0,4,0,0" /></StackPanel><ui:SymbolIcon Grid.Column="1" Symbol="Storage24" FontSize="24" Foreground="{DynamicResource AccentFillColorDefaultBrush}" /></Grid>
                                    </Border>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <Border Style="{StaticResource WorkbenchPanelStyle}" Margin="0,0,0,12">
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource Configuration}" Style="{StaticResource SectionTitleStyle}" />
                                            <Grid Margin="0,4,0,0">
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition /></Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions><RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition /></Grid.RowDefinitions>
                                                <TextBlock Text="{DynamicResource Name}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                                                <TextBlock Grid.Column="1" Text="{Binding Name}" />
                                                <TextBlock Grid.Row="1" Text="{DynamicResource Image}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" Margin="0,8,0,0" />
                                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Image}" Margin="0,8,0,0" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Grid.Row="2" Text="{DynamicResource Created}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" Margin="0,8,0,0" />
                                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Created}" Margin="0,8,0,0" />
                                                <TextBlock Grid.Row="3" Text="{DynamicResource State}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" Margin="0,8,0,0" />
                                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding State}" Margin="0,8,0,0" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource WorkbenchPanelStyle}">
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource Mounts}" Style="{StaticResource SectionTitleStyle}" />
                                            <TextBlock Text="{DynamicResource MountsHint}" Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" />
                                            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                                <TextBlock Text="{DynamicResource Ports}" FontFamily="Consolas" />
                                                <TextBlock Text=": " FontFamily="Consolas" />
                                                <TextBlock Text="{Binding Ports}" FontFamily="Consolas" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="{DynamicResource Logs}">
                        <Grid Margin="0,14,0,0">
                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition /></Grid.RowDefinitions>
                            <WrapPanel Margin="0,0,0,10">
                                <ui:Button Content="{DynamicResource Refresh}" Command="{Binding DataContext.ShowLogsCommand, ElementName=RootPage}" Margin="0,0,8,0" />
                                <ui:Button Content="{DynamicResource FollowLogs}" Command="{Binding DataContext.FollowLogsCommand, ElementName=RootPage}" Margin="0,0,8,0" />
                                <ui:Button Content="{DynamicResource StopFollowing}" Command="{Binding DataContext.StopFollowingLogsCommand, ElementName=RootPage}" />
                            </WrapPanel>
                            <ui:TextBox Grid.Row="1" Text="{Binding DataContext.DetailOutput, ElementName=RootPage}" IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
                        </Grid>
                    </TabItem>
                    <TabItem Header="{DynamicResource Exec}">
                        <Grid Margin="0,14,0,0">
                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition /></Grid.RowDefinitions>
                            <DockPanel Margin="0,0,0,10">
                                <ui:Button DockPanel.Dock="Right" Content="{DynamicResource Exec}" Icon="{ui:SymbolIcon Symbol=ChevronRight24}" Appearance="Primary" Command="{Binding DataContext.ExecCommand, ElementName=RootPage}" Margin="8,0,0,0" />
                                <ui:TextBox Text="{Binding DataContext.ExecText, ElementName=RootPage, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas" />
                            </DockPanel>
                            <ui:TextBox Grid.Row="1" Text="{Binding DataContext.DetailOutput, ElementName=RootPage}" IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
                        </Grid>
                    </TabItem>
                    <TabItem Header="{DynamicResource Inspect}">
                        <Grid Margin="0,14,0,0">
                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition /></Grid.RowDefinitions>
                            <WrapPanel Margin="0,0,0,10">
                                <ui:Button Content="{DynamicResource Inspect}" Appearance="Primary" Command="{Binding DataContext.InspectContainerCommand, ElementName=RootPage}" Margin="0,0,8,0" />
                                <ui:Button Content="{DynamicResource Export}" Command="{Binding DataContext.ExportContainerCommand, ElementName=RootPage}" />
                            </WrapPanel>
                            <ui:TextBox Grid.Row="1" Text="{Binding DataContext.DetailOutput, ElementName=RootPage}" IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
                        </Grid>
                    </TabItem>
                    <TabItem Header="{DynamicResource Metrics}">
                        <Border Style="{StaticResource WorkbenchPanelStyle}" Margin="0,14,0,0" Padding="0">
                            <DataGrid ItemsSource="{Binding DataContext.Stats, ElementName=RootPage}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="{DynamicResource Name}" Binding="{Binding Name}" Width="*" />
                                    <DataGridTextColumn Header="{DynamicResource Cpu}" Binding="{Binding Cpu}" Width="110" />
                                    <DataGridTextColumn Header="{DynamicResource Memory}" Binding="{Binding Memory}" Width="170" />
                                    <DataGridTextColumn Header="{DynamicResource NetworkIo}" Binding="{Binding NetworkIo}" Width="170" />
                                    <DataGridTextColumn Header="{DynamicResource Pids}" Binding="{Binding Pids}" Width="90" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </TabItem>
                </TabControl>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Margin="12">
        <ContentControl Content="{Binding SelectedContainer}">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate" Value="{StaticResource ContainerDetailTemplate}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedContainer}" Value="{x:Null}">
                            <Setter Property="ContentTemplate" Value="{StaticResource ContainerListTemplate}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsCreatingContainer}" Value="True">
                            <Setter Property="ContentTemplate" Value="{StaticResource ContainerCreateTemplate}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</Page>
