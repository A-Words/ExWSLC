<Page x:Class="ExWSLC.Views.Pages.ResourcesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:designViewModels="clr-namespace:ExWSLC.ViewModels.Design"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=designViewModels:DesignResourcesViewModel, IsDesignTimeCreatable=True}"
      Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,18,24,28">
            <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{DynamicResource ResourceTitle}" Style="{StaticResource PageTitleStyle}"/><TextBlock Text="{DynamicResource ResourcesSubtitle}" Style="{StaticResource PageSubtitleStyle}"/></StackPanel><ui:Button Grid.Column="1" Content="{DynamicResource Refresh}" Icon="{ui:SymbolIcon Symbol=ArrowSync24}" Command="{Binding RefreshAllCommand}" Appearance="Secondary" VerticalAlignment="Top"/></Grid>
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,14"><Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="280"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{DynamicResource CreateResource}" FontSize="17"/><TextBlock Text="{DynamicResource CreateResourceHint}" Style="{StaticResource MutedTextStyle}" Margin="0,3,0,0"/></StackPanel><ui:TextBox Grid.Column="1" Text="{Binding ResourceName}" PlaceholderText="resource-name" Margin="16,0"/><WrapPanel Grid.Column="2"><ui:Button Content="{DynamicResource CreateNetwork}" Command="{Binding CreateNetworkCommand}" Appearance="Primary"/><ui:Button Content="{DynamicResource CreateVolume}" Command="{Binding CreateVolumeCommand}"/></WrapPanel></Grid></Border>
            <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                <Border Style="{StaticResource CardStyle}" Margin="0,0,7,0"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="260"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions><ui:FontIcon FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xE774;" FontSize="24" Margin="0,0,12,0"/><StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource Networks}" FontSize="18"/><TextBlock Text="{DynamicResource NetworksHint}" Style="{StaticResource MutedTextStyle}"/></StackPanel></Grid><ui:ListView Grid.Row="1" ItemsSource="{Binding Networks}" SelectedItem="{Binding SelectedNetwork}" Margin="0,14,0,10"><ui:ListView.ItemTemplate><DataTemplate><Grid Margin="3,6"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><Border Width="7" Height="7" CornerRadius="4" Background="{ui:ThemeResource SystemFillColorSuccessBrush}" Margin="0,0,10,0"/><StackPanel Grid.Column="1"><TextBlock Text="{Binding Name}" FontSize="14"/><TextBlock Text="{Binding Id}" Style="{StaticResource MutedTextStyle}" TextTrimming="CharacterEllipsis"/></StackPanel><Border Grid.Column="2" Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" CornerRadius="4" Padding="8,3"><TextBlock Text="{Binding Driver}"/></Border></Grid></DataTemplate></ui:ListView.ItemTemplate></ui:ListView><WrapPanel Grid.Row="2" HorizontalAlignment="Right"><ui:Button Content="{DynamicResource Inspect}" Command="{Binding InspectNetworkCommand}"/><ui:Button Content="{DynamicResource Remove}" Command="{Binding RemoveNetworkCommand}"/><ui:Button Content="{DynamicResource Prune}" Command="{Binding PruneCommand}" CommandParameter="network"/></WrapPanel></Grid></Border>
                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="7,0,0,0"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="260"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions><ui:FontIcon FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xEDA2;" FontSize="24" Margin="0,0,12,0"/><StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource Volumes}" FontSize="18"/><TextBlock Text="{DynamicResource VolumesHint}" Style="{StaticResource MutedTextStyle}"/></StackPanel></Grid><ui:ListView Grid.Row="1" ItemsSource="{Binding Volumes}" SelectedItem="{Binding SelectedVolume}" Margin="0,14,0,10"><ui:ListView.ItemTemplate><DataTemplate><Grid Margin="3,6"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ui:FontIcon Glyph="&#xEDA2;" FontFamily="{DynamicResource SegoeFluentIcons}" FontSize="16" Margin="0,0,10,0"/><StackPanel Grid.Column="1"><TextBlock Text="{Binding Name}" FontSize="14"/><TextBlock Text="{Binding Mountpoint}" Style="{StaticResource MutedTextStyle}" TextTrimming="CharacterEllipsis"/></StackPanel><Border Grid.Column="2" Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" CornerRadius="4" Padding="8,3"><TextBlock Text="{Binding Driver}"/></Border></Grid></DataTemplate></ui:ListView.ItemTemplate></ui:ListView><WrapPanel Grid.Row="2" HorizontalAlignment="Right"><ui:Button Content="{DynamicResource Inspect}" Command="{Binding InspectVolumeCommand}"/><ui:Button Content="{DynamicResource Remove}" Command="{Binding RemoveVolumeCommand}"/><ui:Button Content="{DynamicResource Prune}" Command="{Binding PruneCommand}" CommandParameter="volume"/></WrapPanel></Grid></Border>
            </Grid>
            <TextBlock Text="{DynamicResource Output}" Style="{StaticResource SectionTitleStyle}" Margin="0,20,0,10"/><ui:TextBox Text="{Binding DetailOutput}" IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas" MinHeight="170" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
        </StackPanel>
    </ScrollViewer>
</Page>
