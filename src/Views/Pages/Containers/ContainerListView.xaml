<UserControl x:Class="ExWSLC.Views.Pages.Containers.ContainerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
    <UserControl.Resources>
        <SolidColorBrush x:Key="ContainerDangerIconBrush" Color="#C42B1C" />
        <SolidColorBrush x:Key="ContainerDangerHoverBrush" Color="#18C42B1C" />
        <SolidColorBrush x:Key="ContainerDangerPressedBrush" Color="#28C42B1C" />

        <Style x:Key="ContainerCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ui:ThemeResource ControlFillColorSecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{ui:ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="18,14" />
            <Setter Property="Margin" Value="0,4,0,8" />
        </Style>

        <Style x:Key="ContainerActionButtonStyle" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="ContainerDeleteActionButtonStyle" TargetType="ui:Button" BasedOn="{StaticResource ContainerActionButtonStyle}">
            <Setter Property="MouseOverBackground" Value="{StaticResource ContainerDangerHoverBrush}" />
            <Setter Property="MouseOverBorderBrush" Value="{StaticResource ContainerDangerHoverBrush}" />
            <Setter Property="PressedBackground" Value="{StaticResource ContainerDangerPressedBrush}" />
            <Setter Property="PressedBorderBrush" Value="{StaticResource ContainerDangerPressedBrush}" />
        </Style>

        <DataTemplate x:Key="ContainerListItemTemplate">
            <Border Style="{StaticResource ContainerCardStyle}" Cursor="Hand">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick"
                                  Command="{Binding DataContext.ShowContainerDetailsFromListCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  CommandParameter="{Binding Container}" />
                </Border.InputBindings>
                <Grid MinHeight="60">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <Border Width="8" Height="8" CornerRadius="4" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,8,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{ui:ThemeResource TextFillColorTertiaryBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Background" Value="{ui:ThemeResource SystemFillColorSuccessBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>

                        <StackPanel Grid.Column="1" MinWidth="0" Margin="0,0,14,0">
                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Image}" Style="{StaticResource MutedTextStyle}" FontSize="13" TextTrimming="CharacterEllipsis" Margin="0,4,0,0" />

                            <WrapPanel Margin="0,8,0,0">
                                <StackPanel Orientation="Horizontal" Margin="0,0,18,0">
                                    <TextBlock Text="{DynamicResource Ports}" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                                    <TextBlock Text="{Binding Ports}" FontSize="12" TextTrimming="CharacterEllipsis" MaxWidth="180" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,18,0">
                                    <TextBlock Text="{DynamicResource Cpu}" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                                    <TextBlock Text="{Binding Cpu}" FontSize="12" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{DynamicResource Memory}" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                                    <TextBlock Text="{Binding Memory}" FontSize="12" />
                                </StackPanel>
                            </WrapPanel>
                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="18,0,0,0">
                        <ui:Button Icon="{ui:SymbolIcon Symbol=Play24}" Appearance="Secondary" Command="{Binding DataContext.StartContainerFromListCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Container}" ToolTip="{DynamicResource Start}">
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource ContainerActionButtonStyle}">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                        <ui:Button Appearance="Secondary" Command="{Binding DataContext.StopContainerFromListCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Container}" ToolTip="{DynamicResource Stop}">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Stop24" Filled="True" />
                            </ui:Button.Icon>
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource ContainerActionButtonStyle}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                        <ui:Button Icon="{ui:SymbolIcon Symbol=MoreVertical24}"
                                   Appearance="Secondary"
                                   ToolTip="{DynamicResource Actions}"
                                   Style="{StaticResource ContainerActionButtonStyle}"
                                   Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   Click="MoreActionsButton_Click">
                            <ui:Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="{DynamicResource Restart}"
                                              Command="{Binding PlacementTarget.Tag.RestartContainerFromListCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.DataContext.Container, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="{DynamicResource Details}"
                                              Command="{Binding PlacementTarget.Tag.ShowContainerDetailsFromListCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.DataContext.Container, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="{DynamicResource Terminal}"
                                              Command="{Binding PlacementTarget.Tag.OpenTerminalFromListCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.DataContext.Container, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                </ContextMenu>
                            </ui:Button.ContextMenu>
                        </ui:Button>
                        <ui:Button Appearance="Secondary" Command="{Binding DataContext.RemoveContainerFromListCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Container}" ToolTip="{DynamicResource Remove}" Style="{StaticResource ContainerDeleteActionButtonStyle}">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Delete24" Foreground="{StaticResource ContainerDangerIconBrush}" />
                            </ui:Button.Icon>
                        </ui:Button>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox PlaceholderText="{DynamicResource Search}" ClearButtonEnabled="True" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" MinHeight="28" VerticalContentAlignment="Center" />
            <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon Symbol=ArrowSync24}" Appearance="Secondary" Command="{Binding RefreshAllCommand}" Width="28" Height="28" Padding="0" Margin="8,0,0,0" ToolTip="{DynamicResource Refresh}" />
            <ui:Button Grid.Column="2" Icon="{ui:SymbolIcon Symbol=Add24}" Appearance="Secondary" Command="{Binding ShowCreateContainerCommand}" Width="28" Height="28" Padding="0" Margin="8,0,0,0" ToolTip="{DynamicResource CreateContainer}" />
        </Grid>

        <ScrollViewer Grid.Row="1"
                      Margin="0,14,0,0"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding VisibleContainerItems}"
                          ItemTemplate="{StaticResource ContainerListItemTemplate}" />
        </ScrollViewer>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="360">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding VisibleContainerItems.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>

            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SearchText}" Value="">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="{DynamicResource NoContainersTitle}" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,7" />
                <TextBlock Text="{DynamicResource NoContainersHint}" TextWrapping="Wrap" TextAlignment="Center" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                <ui:Button Content="{DynamicResource CreateContainer}" Icon="{ui:SymbolIcon Symbol=Add24}" Appearance="Primary" Command="{Binding ShowCreateContainerCommand}" HorizontalAlignment="Center" Margin="0,18,0,0" />
            </StackPanel>

            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SearchText}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="{DynamicResource NoContainerResultsTitle}" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,7" />
                <TextBlock Text="{DynamicResource NoContainerResultsHint}" TextWrapping="Wrap" TextAlignment="Center" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
