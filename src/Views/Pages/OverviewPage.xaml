<Page x:Class="ExWSLC.Views.Pages.OverviewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:models="clr-namespace:ExWSLC.Models"
      xmlns:designViewModels="clr-namespace:ExWSLC.ViewModels.Design"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=designViewModels:DesignOverviewPageViewModel, IsDesignTimeCreatable=True}"
      Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
    <Page.Resources>
        <Style x:Key="DashboardPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{ui:ThemeResource ControlFillColorSecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{ui:ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="18" />
        </Style>
        <Style x:Key="StepNumberStyle" TargetType="Border">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="CornerRadius" Value="17" />
            <Setter Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Margin" Value="0,0,14,0" />
        </Style>
        <DataTemplate x:Key="NoCurrentTaskTemplate">
            <Grid MinHeight="140">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="30" Foreground="{ui:ThemeResource SystemFillColorSuccessBrush}" />
                    <TextBlock Text="{DynamicResource NoCurrentTask}" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,12,0,4" />
                    <TextBlock Text="{DynamicResource NoCurrentTaskHint}" Style="{StaticResource MutedTextStyle}" TextAlignment="Center" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="CurrentTaskTemplate">
            <StackPanel MinHeight="140">
                <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="{Binding Detail}" Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" Margin="0,7,0,14" MaxHeight="40" />
                <ProgressBar Height="4" IsIndeterminate="True" />
                <Grid Margin="0,14,0,0">
                    <TextBlock Text="{Binding StartedAt, StringFormat={}{0:t}}" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center" />
                    <ui:Button Content="{DynamicResource Cancel}" Appearance="Secondary" Command="{Binding DataContext.CancelCurrentOperationCommand, RelativeSource={RelativeSource AncestorType=Page}}" HorizontalAlignment="Right" />
                </Grid>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,24,32,32" MaxWidth="1180">
            <Border Style="{StaticResource DashboardPanelStyle}" Padding="22" Margin="0,0,0,22" BorderBrush="{DynamicResource AccentFillColorDefaultBrush}">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <Border Width="48" Height="48" CornerRadius="24" Background="{ui:ThemeResource SystemFillColorSuccessBrush}" Margin="0,0,16,0">
                        <ui:SymbolIcon Symbol="Checkmark24" FontSize="25" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <Image Grid.Column="1" Source="/Assets/wslc-ready-illustration.png" Width="300" Height="112" Stretch="Uniform" Opacity="0.24" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,-16,24,-16" IsHitTestVisible="False" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource RuntimeReadyTitle}" FontSize="22" FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource RuntimeReadyHint}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" Margin="0,5,0,0" />
                        <ui:Button Content="{DynamicResource ViewPrerequisites}" Appearance="Secondary" Background="Transparent" BorderThickness="0" Click="OpenResources_Click" HorizontalAlignment="Left" Padding="0" Margin="0,6,0,0" />
                    </StackPanel>
                    <ui:Button Grid.Column="2" Content="{DynamicResource CheckAgain}" Icon="{ui:SymbolIcon Symbol=ArrowSync24}" Appearance="Secondary" Command="{Binding RefreshAllCommand}" VerticalAlignment="Top" Padding="14,7" />
                </Grid>
            </Border>

            <TextBlock Text="{DynamicResource GetStartedTitle}" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="{DynamicResource GetStartedSubtitle}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" Margin="0,4,0,16" />

            <Border Style="{StaticResource DashboardPanelStyle}" Margin="0,0,0,14" Padding="22">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="32"/><ColumnDefinition/><ColumnDefinition Width="32"/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource StepNumberStyle}"><TextBlock Text="1" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource ChooseImage}" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="{DynamicResource ChooseImageHint}" Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" Margin="0,4,0,12"/><ui:Button Content="{DynamicResource ChooseImage}" Appearance="Secondary" Click="OpenImages_Click" HorizontalAlignment="Left"/></StackPanel>
                    </Grid>
                    <ui:SymbolIcon Grid.Column="1" Symbol="ChevronRight24" Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" />
                    <Grid Grid.Column="2">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource StepNumberStyle}"><TextBlock Text="2" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource Configure}" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="{DynamicResource ConfigureHint}" Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" Margin="0,4,0,12"/><ui:Button Content="{DynamicResource Configure}" Appearance="Secondary" Click="ConfigureContainer_Click" HorizontalAlignment="Left"/></StackPanel>
                    </Grid>
                    <ui:SymbolIcon Grid.Column="3" Symbol="ChevronRight24" Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" />
                    <Grid Grid.Column="4">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource StepNumberStyle}"><TextBlock Text="3" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource RunContainer}" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="{DynamicResource RunContainerHint}" Style="{StaticResource MutedTextStyle}" TextWrapping="Wrap" Margin="0,4,0,12"/><ui:Button Content="{DynamicResource RunContainer}" Icon="{ui:SymbolIcon Symbol=Play24}" Appearance="Primary" Click="ConfigureContainer_Click" HorizontalAlignment="Left"/></StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions><ColumnDefinition Width="1.15*"/><ColumnDefinition Width="14"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <Border Style="{StaticResource DashboardPanelStyle}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
                        <Grid Margin="18,14,14,8"><TextBlock Text="{DynamicResource ActiveContainers}" FontSize="16" FontWeight="SemiBold"/><ui:Button Content="{DynamicResource ViewAll}" Appearance="Secondary" Background="Transparent" BorderThickness="0" Click="OpenContainers_Click" HorizontalAlignment="Right" Padding="4"/></Grid>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding ActiveContainers}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="0" MinHeight="180">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="name" Width="1.1*"><DataGridTemplateColumn.HeaderTemplate><DataTemplate><TextBlock Text="{DynamicResource Name}"/></DataTemplate></DataGridTemplateColumn.HeaderTemplate><DataGridTemplateColumn.CellTemplate><DataTemplate><ui:Button Appearance="Secondary" Background="Transparent" BorderThickness="0" Click="OpenContainer_Click" HorizontalAlignment="Left" Padding="0"><StackPanel Orientation="Horizontal"><Border Width="6" Height="6" CornerRadius="3" Background="{ui:ThemeResource SystemFillColorSuccessBrush}" Margin="0,0,8,0"/><TextBlock Text="{Binding Name}" FontWeight="SemiBold"/></StackPanel></ui:Button></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                                <DataGridTextColumn Header="image" Binding="{Binding Image}" Width="1.6*"><DataGridTextColumn.HeaderTemplate><DataTemplate><TextBlock Text="{DynamicResource Image}"/></DataTemplate></DataGridTextColumn.HeaderTemplate></DataGridTextColumn>
                                <DataGridTextColumn Header="state" Binding="{Binding State}" Width=".65*"><DataGridTextColumn.HeaderTemplate><DataTemplate><TextBlock Text="{DynamicResource State}"/></DataTemplate></DataGridTextColumn.HeaderTemplate></DataGridTextColumn>
                                <DataGridTextColumn Header="status" Binding="{Binding Status}" Width=".85*"><DataGridTextColumn.HeaderTemplate><DataTemplate><TextBlock Text="{DynamicResource Status}"/></DataTemplate></DataGridTextColumn.HeaderTemplate></DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource DashboardPanelStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource CurrentTask}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <ContentControl Content="{Binding ActiveTask}">
                            <ContentControl.Style><Style TargetType="ContentControl"><Setter Property="ContentTemplate" Value="{StaticResource CurrentTaskTemplate}"/><Style.Triggers><Trigger Property="Content" Value="{x:Null}"><Setter Property="ContentTemplate" Value="{StaticResource NoCurrentTaskTemplate}"/></Trigger></Style.Triggers></Style></ContentControl.Style>
                        </ContentControl>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="14"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <Border Style="{StaticResource DashboardPanelStyle}">
                    <StackPanel>
                        <Grid Margin="0,0,0,8"><TextBlock Text="{DynamicResource RecentTasks}" FontSize="16" FontWeight="SemiBold"/><ui:Button Content="{DynamicResource ViewAll}" Appearance="Secondary" Background="Transparent" BorderThickness="0" Click="OpenTasks_Click" HorizontalAlignment="Right" Padding="4"/></Grid>
                        <ItemsControl ItemsSource="{Binding RecentTasks}">
                            <ItemsControl.ItemTemplate><DataTemplate DataType="{x:Type models:RuntimeTaskItem}"><Grid Margin="0,5"><Grid.ColumnDefinitions><ColumnDefinition Width="18"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="15" Foreground="{ui:ThemeResource SystemFillColorSuccessBrush}" VerticalAlignment="Top"/><StackPanel Grid.Column="1"><TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Detail}" Style="{StaticResource MutedTextStyle}" TextTrimming="CharacterEllipsis"/></StackPanel><TextBlock Grid.Column="2" Text="{Binding StartedAt, StringFormat={}{0:t}}" Style="{StaticResource MutedTextStyle}" Margin="12,0,0,0"/></Grid></DataTemplate></ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource DashboardPanelStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Prerequisites}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <Grid Margin="0,7"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ui:SymbolIcon Symbol="CheckmarkCircle24" Foreground="{ui:ThemeResource SystemFillColorSuccessBrush}"/><StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource WslInstalled}"/><TextBlock Text="{DynamicResource WslInstalledHint}" Style="{StaticResource MutedTextStyle}"/></StackPanel><TextBlock Grid.Column="2" Text="{Binding Capabilities.CliVersion}" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center" MaxWidth="150" TextTrimming="CharacterEllipsis"/></Grid>
                        <Separator Margin="0,4"/>
                        <Grid Margin="0,7"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ui:SymbolIcon Symbol="CheckmarkCircle24" Foreground="{ui:ThemeResource SystemFillColorSuccessBrush}"/><StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource ContainerRuntime}"/><TextBlock Text="{DynamicResource ContainerRuntimeHint}" Style="{StaticResource MutedTextStyle}"/></StackPanel><TextBlock Grid.Column="2" Text="{Binding Capabilities.Message}" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center" MaxWidth="150" TextTrimming="CharacterEllipsis"/></Grid>
                        <Separator Margin="0,4"/>
                        <Grid Margin="0,7"><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ui:SymbolIcon Symbol="CheckmarkCircle24" Foreground="{ui:ThemeResource SystemFillColorSuccessBrush}"/><StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource SdkIntegration}"/><TextBlock Text="{DynamicResource SdkIntegrationHint}" Style="{StaticResource MutedTextStyle}"/></StackPanel><TextBlock Grid.Column="2" Text="{Binding Capabilities.SdkVersion}" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center"/></Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
