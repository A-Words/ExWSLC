<Page x:Class="ExWSLC.Views.Pages.ImagesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:designViewModels="clr-namespace:ExWSLC.ViewModels.Design"
      x:Name="RootPage"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=designViewModels:DesignImagesViewModel, IsDesignTimeCreatable=True}"
      Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <DataTemplate x:Key="ImageListTemplate">
            <Grid Margin="2,6"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions><ui:FontIcon FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xE7B8;" FontSize="17" Margin="0,0,10,0"/><StackPanel Grid.Column="1"><TextBlock Text="{Binding DisplayName}" FontSize="13" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding DisplaySize}" Style="{StaticResource MutedTextStyle}"/></StackPanel></Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="{DynamicResource ImagesTitle}" Style="{StaticResource PageTitleStyle}"/>
                <TextBlock Text="{DynamicResource ImagesSubtitle}" Style="{StaticResource PageSubtitleStyle}"/>
            </StackPanel>
            <ui:Button Grid.Column="1" Content="{DynamicResource Refresh}" Icon="{ui:SymbolIcon Symbol=ArrowSync24}" Command="{Binding RefreshAllCommand}" Appearance="Secondary" VerticalAlignment="Top"/>
        </Grid>

        <Expander Grid.Row="1" Header="{DynamicResource GeneralImageOperations}" Margin="0,14,0,0">
            <StackPanel Margin="0,8,0,0">
                    <Border Style="{StaticResource CompactCardStyle}" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="260"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <ui:SymbolIcon Symbol="ArrowDownload24" FontSize="23" Margin="0,0,14,0"/>
                            <StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource Pull}" FontSize="16"/><TextBlock Text="{DynamicResource PullHint}" Style="{StaticResource MutedTextStyle}"/></StackPanel>
                            <ui:TextBox Grid.Column="2" Text="{Binding ImageReference}" PlaceholderText="ubuntu:latest" VerticalAlignment="Center" Margin="12,0"/>
                            <ui:Button Grid.Column="3" Content="{DynamicResource Pull}" Appearance="Primary" Command="{Binding PullImageCommand}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <Expander>
                            <Expander.Header><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions><ui:SymbolIcon Symbol="Code24" FontSize="23" Margin="0,0,14,0"/><StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource Build}" FontSize="16"/><TextBlock Text="{DynamicResource BuildHint}" Style="{StaticResource MutedTextStyle}"/></StackPanel></Grid></Expander.Header>
                            <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0"><Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel Margin="0,0,12,0"><TextBlock Text="Build context"/><ui:TextBox Text="{Binding BuildContextPath}" PlaceholderText="C:\src\app" Margin="0,5,0,10"/><TextBlock Text="Dockerfile"/><ui:TextBox Text="{Binding DockerfilePath}" PlaceholderText="Optional custom path" Margin="0,5,0,0"/></StackPanel><StackPanel Grid.Column="1"><TextBlock Text="Image tag"/><ui:TextBox Text="{Binding BuildImageTag}" PlaceholderText="my-app:latest" Margin="0,5,0,12"/><ui:Button Content="{DynamicResource Build}" Icon="{ui:SymbolIcon Symbol=Play24}" Appearance="Primary" Command="{Binding BuildImageCommand}" HorizontalAlignment="Right"/></StackPanel></Grid></Border>
                        </Expander>
                        <Expander Grid.Column="2">
                            <Expander.Header><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions><ui:SymbolIcon Symbol="Archive24" FontSize="23" Margin="0,0,14,0"/><StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource ImageFiles}" FontSize="16"/><TextBlock Text="{DynamicResource ImageFilesHint}" Style="{StaticResource MutedTextStyle}"/></StackPanel></Grid></Expander.Header>
                            <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel Margin="0,0,12,0"><TextBlock Text="{DynamicResource ArchivePath}"/><ui:TextBox Text="{Binding ArchivePath}" PlaceholderText="C:\images\image.tar" Margin="0,5,0,0"/></StackPanel><StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource ImportImageName}"/><ui:TextBox Text="{Binding ImportImageName}" PlaceholderText="imported:latest" Margin="0,5,0,0"/></StackPanel><WrapPanel Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,12,0,0"><ui:Button Content="{DynamicResource Import}" Command="{Binding ImportImageCommand}"/><ui:Button Content="{DynamicResource Load}" Command="{Binding LoadImageCommand}"/></WrapPanel></Grid></Border>
                        </Expander>
                    </Grid>
            </StackPanel>
        </Expander>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions><ColumnDefinition Width="260" MinWidth="210" MaxWidth="330"/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <Border Style="{StaticResource LayerCardStyle}" Margin="0" Padding="0">
                <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <ui:TextBox Margin="8" PlaceholderText="{DynamicResource SearchImages}" ClearButtonEnabled="True" Text="{Binding ImageSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    <ui:ListView Grid.Row="1" ItemsSource="{Binding VisibleImages}" SelectedItem="{Binding SelectedImage}" ItemTemplate="{StaticResource ImageListTemplate}" Margin="4,0"/>
                    <ui:Button Grid.Row="2" Content="{DynamicResource PruneDanglingImages}" Icon="{ui:SymbolIcon Symbol=Delete24}" Appearance="Secondary" Command="{Binding PruneCommand}" CommandParameter="image" Margin="8" HorizontalAlignment="Stretch"/>
                </Grid>
            </Border>
            <GridSplitter Grid.Column="1" Width="12" Background="Transparent"/>
            <Border Grid.Column="2" Style="{StaticResource LayerCardStyle}" Margin="0" Padding="0" ClipToBounds="True">
                <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="24">
                    <TextBlock Text="{DynamicResource SelectedImageActions}" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,10"/>
                    <Border Style="{StaticResource CardStyle}" Padding="24" Margin="0,0,0,14">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                <Border Width="76" Height="76" CornerRadius="14" Background="{ui:ThemeResource ControlFillColorTertiaryBrush}" Margin="0,0,18,0"><ui:FontIcon FontFamily="{DynamicResource SegoeFluentIcons}" Glyph="&#xE7B8;" FontSize="34" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="{Binding SelectedImageDisplayName}" FontSize="24" FontWeight="SemiBold"/><TextBlock Text="{Binding SelectedImage.Id}" FontFamily="Consolas" Style="{StaticResource MutedTextStyle}" TextTrimming="CharacterEllipsis" Margin="0,6,0,0"/><WrapPanel Margin="0,8,0,0"><TextBlock Text="{DynamicResource Size}" Style="{StaticResource MutedTextStyle}"/><TextBlock Text=": " Style="{StaticResource MutedTextStyle}"/><TextBlock Text="{Binding SelectedImage.DisplaySize}" Style="{StaticResource MutedTextStyle}" Margin="0,0,18,0"/><TextBlock Text="{DynamicResource Created}" Style="{StaticResource MutedTextStyle}"/><TextBlock Text=": " Style="{StaticResource MutedTextStyle}"/><TextBlock Text="{Binding SelectedImage.DisplayCreated}" Style="{StaticResource MutedTextStyle}"/></WrapPanel></StackPanel>
                            </Grid>
                            <WrapPanel Margin="0,18,0,0" HorizontalAlignment="Right">
                                <ui:Button Content="{DynamicResource Inspect}" Command="{Binding InspectImageCommand}" IsEnabled="{Binding HasSelectedImage}" Appearance="Secondary" Margin="0,0,8,0"/>
                                <ui:Button Content="{DynamicResource Save}" Command="{Binding SaveImageCommand}" IsEnabled="{Binding HasSelectedImage}" Margin="0,0,8,0"/>
                                <ui:Button Content="{DynamicResource Push}" Command="{Binding PushImageCommand}" IsEnabled="{Binding HasSelectedImage}" Margin="0,0,8,0"/>
                                <ui:Button Content="{DynamicResource Remove}" Command="{Binding RemoveImageCommand}" IsEnabled="{Binding HasSelectedImage}"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CompactCardStyle}" Margin="0,0,0,14"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="220"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ui:SymbolIcon Symbol="Tag24" FontSize="23" Margin="0,0,14,0"/><StackPanel Grid.Column="1"><TextBlock Text="{DynamicResource Tag}" FontSize="16"/><TextBlock Text="{DynamicResource TagHint}" Style="{StaticResource MutedTextStyle}"/></StackPanel><ui:TextBox Grid.Column="2" Text="{Binding ImageTag}" PlaceholderText="registry/name:tag" VerticalAlignment="Center" Margin="12,0" IsEnabled="{Binding HasSelectedImage}"/><ui:Button Grid.Column="3" Content="{DynamicResource Tag}" Command="{Binding TagImageCommand}" IsEnabled="{Binding HasSelectedImage}"/></Grid></Border>

                    <StackPanel Visibility="{Binding HasImageInspectOutput, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="{DynamicResource ImageInspectOutput}" Style="{StaticResource SectionTitleStyle}" Margin="0,8,0,10"/>
                        <ui:TextBox Text="{Binding ImageInspectOutput, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Consolas" Height="220" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Auto"/>
                    </StackPanel>
                </StackPanel></ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
